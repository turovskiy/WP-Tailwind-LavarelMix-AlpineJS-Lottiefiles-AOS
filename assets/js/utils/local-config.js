// Цей файл включає лише локальний конфігураційний файл, якщо він існує.
// Запобігає помилкам у збірці у віддалені середовища, де Локальний файл недоступний.

const fs = require('fs');

// Якщо нам потрібно додати значення за замовчуванням / у будь-якій точці, додайте їх тут.
let config = {};

if (fs.existsSync('./local.json')) {
    config = require('./../../../local.json');
}

module.exports = config;